export default function AssignmentLoading() {
  return (
    <div
      style={{
        display: "grid",
        minHeight: "65vh",
        placeItems: "center",
        color: "var(--tr-ink-muted)",
        fontFamily: "var(--font-geist-mono), monospace",
        fontSize: 11,
      }}
    >
      Preparando escenarios de asignación…
    </div>
  );
}

